{
  "name": "pokemon-prize-ladder",
  "version": "0.2.0",
  "private": true,
  "description": "Vercel-ready Pokemon base-stat higher/lower prize ladder game.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "ev": "node scripts/ev-simulator.mjs",
    "seed:pokemon": "node scripts/build-pokemon-dataset.mjs --write-main",
    "images:pokemon": "node scripts/build-pokemon-dataset.mjs --write-main --download-images",
    "check:conflicts": "node scripts/check-conflicts.mjs",
    "prebuild": "node scripts/check-conflicts.mjs"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "eslint": "latest",
    "eslint-config-next": "latest",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
